{
  "name": "sailboat-simulator",
  "private": true,
  "version": "0.1.0",
  "description": "High-fidelity browser-based sailboat simulator — Three.js + Rapier",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node scripts/test-buoyancy.mjs && node scripts/test-sailing.mjs && node scripts/test-cloth.mjs && node scripts/test-fft.mjs"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0",
    "lil-gui": "^0.19.2",
    "three": "^0.166.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
